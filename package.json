{
  "name": "sennheiser-store-locator-app",
  "version": "1.0.0",
  "main": "web/index.js",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "npm install --prefix web/frontend && npm run build --prefix web/frontend",
    "dev": "npm run dev --prefix web",
    "start": "npm run serve --prefix web",
    "serve": "npm run serve --prefix web",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {
    "@shopify/polaris": "^13.9.5",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "multer": "^2.1.1",
    "xlsx": "^0.18.5"
  },
  "author": "PCA177",
  "private": true,
  "workspaces": [
    "extensions/*",
    "web",
    "web/frontend"
  ]
}